<!DOCTYPE html>
<!DOCTYPE html>
<html> 
    <header> <title>Filmoteca </title> </header> 
    <body>
        <h1> Bienvenue </h1>
        <p> Cette phrase est une phrase d'introduction du site</p>
        <link rel="stylesheet" href="Page_acceuille_css.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel ="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

        <nav> Nous proposons : <br>
            

    <table class="table table-striped"> 
    <thead>
        <th> Titre</th> 
        <th> Année</th> 
        <th> Genre</th> 
        <th> Synopsis</th> 
        <th> Director</th> 
        <th> Supprimer à</th> 
        <th> Créer à</th> 
        <th> Mise à jour à</th> 
    </thead>

        <tbody> 
        {% for film in films %}
        <tr>
            <td> {{film.title}}</td>
            <td> {{film.year}}</td>
            <td> {{film.type}}</td>
            <td> {{film.synopsis}}</td>
            <td> {{film.director}}</td>
            <td> {{film.deletedAt ? film.deletedAt|date('Y-m-d H:i:s'):' Aucunde date'}}</td>
            <td> {{film.createdAt ? film.createdAt|date('Y-m-d H:i:s'):' Aucunde date'}}</td>
            <td> {{film.updtatedAt ? film.updtatedAt|date('Y-m-d H:i:s'):' Aucunde date'}}</td>
            </tr>
            {% endfor %}
        </tbody>
        
    </table>

    
    
</body>
<footer > Nos réseaux : blablabla</footer>
        